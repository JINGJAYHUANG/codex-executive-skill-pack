# Schemas

The JSON Schemas document the public shapes of:

- `catalog/skills.json`;
- `catalog/routes.json`.

The zero-dependency validator performs additional cross-file checks that JSON Schema alone cannot express, including exact order, file parity, frontmatter consistency, explicit-invocation metadata, and route acyclicity.
